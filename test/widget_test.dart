import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:multi_s/main.dart';

void main() {
  testWidgets('Counter increments smoke test', (WidgetTester tester) async {
    expect(1 + 1, 2);
  });
}
